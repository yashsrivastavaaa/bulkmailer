import { NextResponse } from 'next/server'; import { query } from '@/lib/db'; export const runtime='nodejs'; export async function GET(){const {rows}=await query(`SELECT slug,name,description,price_monthly_cents,currency,monthly_send_limit,max_attachment_mb,max_recipients_per_campaign,max_custom_columns,features FROM plans WHERE is_active=true ORDER BY sort_order`);return NextResponse.json({plans:rows})}
